---
interface Props {
  tag?: 'h1' | 'h2' | 'h3' | 'span' | 'p';
  class?: string;
}

const { tag: Tag = 'span', class: className = '' } = Astro.props;
---

<Tag class={`gradient-text ${className}`}>
  <slot />
</Tag>
